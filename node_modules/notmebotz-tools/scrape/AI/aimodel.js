const Groq = require("groq-sdk");

const groq = new Groq({ apiKey: "" });

async function groqm() {
  const models = await groq.models.list();
    return({
      author: "Herza",
      status: 200,
      result: {
        models
      }
    )};
}

groqm();